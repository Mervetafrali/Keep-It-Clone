import { LetterpipePipe } from './letterpipe.pipe';

describe('LetterpipePipe', () => {
  it('create an instance', () => {
    const pipe = new LetterpipePipe();
    expect(pipe).toBeTruthy();
  });
});
